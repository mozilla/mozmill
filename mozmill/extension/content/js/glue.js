function openFile(){
  utils.openFile(window);
}

function runFile(){
  utils.runFile(window);
}

function runEditor(){
  utils.runEditor(window);
}

function genBoiler(){
  utils.genBoiler(window);
}